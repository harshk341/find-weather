import { Key } from '../constants/apiKey';

const IP_URL = 'https://api.ipgeolocation.io/getip';
const WEATHER_URL = `https://api.weatherapi.com/v1/forecast.json?key=${Key}`;

export {
    IP_URL,
    WEATHER_URL
}